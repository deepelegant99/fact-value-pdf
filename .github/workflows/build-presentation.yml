name: Build Putnam presentation

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/build-presentation.yml'
      - 'scripts/build_presentation.js'
      - 'Content/putnam-fact-value-slides.md'
      - 'Template/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install presentation tools
        run: |
          npm install pptxgenjs
          python -m pip install --upgrade pip
          pip install python-pptx pillow
          sudo apt-get update
          sudo apt-get install -y libreoffice-impress poppler-utils fonts-liberation

      - name: Build readable PowerPoint from Markdown
        run: node scripts/build_presentation.js

      - name: Convert PowerPoint to PDF
        run: |
          soffice --headless --convert-to pdf --outdir Presentation Presentation/putnam-fact-value-slides.pptx

      - name: Quality checks
        run: |
          python - <<'PY'
          from pptx import Presentation
          p='Presentation/putnam-fact-value-slides.pptx'
          prs=Presentation(p)
          assert len(prs.slides) >= 30, len(prs.slides)
          for i in range(min(30, len(prs.slides))):
              slide=prs.slides[i]
              text=' '.join(sh.text for sh in slide.shapes if getattr(sh,'has_text_frame',False)).strip()
              assert text, f'slide {i+1} is empty'
          print('PPTX QA OK:', len(prs.slides), 'slides')
          PY
          pages=$(pdfinfo Presentation/putnam-fact-value-slides.pdf | awk '/^Pages:/ {print $2}')
          test "$pages" -ge 30
          echo "PDF QA OK: $pages pages"

      - name: Render preview pages
        run: |
          mkdir -p build-preview
          pdftoppm -png -r 72 -f 1 -l 30 Presentation/putnam-fact-value-slides.pdf build-preview/slide >/dev/null 2>&1

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: putnam-presentation
          path: |
            Presentation/putnam-fact-value-slides.pptx
            Presentation/putnam-fact-value-slides.pdf
            build-preview/*.png
          retention-days: 7

      - name: Commit generated presentation
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add Presentation/putnam-fact-value-slides.pptx Presentation/putnam-fact-value-slides.pdf
          if ! git diff --cached --quiet; then
            git commit -m 'Build readable Putnam fact-value presentation'
            git push
          fi
